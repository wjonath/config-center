spring:
  # 不要为我们工程中的资源文件建立映射
  resources:
    add-mappings: false
  mvc:
  # 出现错误时, 直接抛出异常
    throw-exception-if-no-handler-found: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6000/eureka/
